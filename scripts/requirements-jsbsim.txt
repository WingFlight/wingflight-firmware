# Pinned Python dependencies for scripts/jsbsim_bridge.py.
# Install into the project venv:
#   python -m venv tools/jsbsim-venv
#   tools/jsbsim-venv/Scripts/pip.exe install -r scripts/requirements-jsbsim.txt
# (or let scripts/sitl-jsbsim-flightgear-launch.ps1 -SetupVenv do both).
#
# Version pinned to the one validated in
# docs/development/SITL JSBSim FlightGear Plan.md section 9. Bump deliberately,
# then re-run: scripts/sitl-rc-check.ps1 -Mode jsbsim -AutoStartSitl -StopSitlOnExit
jsbsim==1.3.1
